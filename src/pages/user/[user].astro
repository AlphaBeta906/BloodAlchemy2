---
import Layout from "@/layouts/Layout.astro";
import Profile from "@/containers/Profile";

const { user } = Astro.params;
---

<Layout title={`User: ${user}`} />
    <Profile username={user ?? ""} client:only />
</Layout>