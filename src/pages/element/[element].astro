---
import Layout from "@/layouts/Layout.astro";
import Element from '@/containers/Element'

const { element } = Astro.params;
---

<Layout title={`Element: ${element}`} />
    <Element name={element ?? ""} client:only />
</Layout>